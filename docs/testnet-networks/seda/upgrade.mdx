---
title: 'Upgrade Guide'
sidebar_position: 2
hide_title: true
hide_table_of_contents: false
description: ''
chain_name: 'Seda'
scope: 'testnet'
chain_id: 'seda-1-testnet'
binary_version: 'v1.0.0-rc.4'
icon_url: 'seda-icon.svg'
---

import PageTitle from "@site/src/components/PageTitle";

<PageTitle
  iconUrl="img/seda-icon.svg"
  title="Seda Upgrade Guide"
  chainId="seda-1-testnet"
  version="v1.0.0-rc.4"
/>

## Clone Seda repository

```js
cd $HOME
rm -rf seda-chain
git clone https://github.com/sedaprotocol/seda-chain.git
cd seda-chain
git checkout v1.0.0-rc.4
```

## Build binaries

```js
make build
```

## Prepare binaries for Cosmovisor

```js
mkdir -p ~/.sedad/cosmovisor/upgrades/v1.0.0-rc.4/bin
mv build/sedad ~/.sedad/cosmovisor/upgrades/v1.0.0-rc.4/bin/
rm -rf build
```

# Without Cosmovisor

## Clone Seda repository

```js
cd $HOME
rm -rf seda-chain
git clone https://github.com/sedaprotocol/seda-chain.git
cd seda-chain
git checkout v1.0.0-rc.4
```

## Build binaries

```js
make install
```

## Restart node and check logs

```js
sudo systemctl restart sedad && sudo journalctl -u sedad -f --no-hostname -o cat
```
