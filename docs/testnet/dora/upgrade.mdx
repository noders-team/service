---
title: 'Upgrade Guide'
sidebar_position: 2
hide_title: true
hide_table_of_contents: false
description: ''
chain_name: 'DoraVota'
scope: 'testnet'
chain_id: 'auto'
binary_version: 'auto'
icon_url: 'dora-icon.svg'
---

import PageTitle from "@site/src/components/PageTitle";

<PageTitle
  iconUrl="img/dora-icon.svg"
  title="DoraVota Upgrade Guide"
  chainId="auto"
  version="auto"
/>

## Clone DoraVota repository

```js
cd $HOME
rm -rf doravota
git clone https://github.com/DoraFactory/doravota.git
cd doravota
git checkout auto
```

## Build binaries

```js
make build
```

## Prepare binaries for Cosmovisor

```js
mkdir -p ~/.dorad/cosmovisor/upgrades/auto/bin
mv build/dorad ~/.dorad/cosmovisor/upgrades/auto/bin/
rm -rf build
```

# Without Cosmovisor

## Clone DoraVota repository

```js
cd $HOME
rm -rf doravota
git clone https://github.com/DoraFactory/doravota.git
cd doravota
git checkout auto
```

## Build binaries

```js
make install
```

## Restart node and check logs

```js
sudo systemctl restart dorad && sudo journalctl -u dorad -f --no-hostname -o cat
```
